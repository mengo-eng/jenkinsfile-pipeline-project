pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself - made some changes'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
